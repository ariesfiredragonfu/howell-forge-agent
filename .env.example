# Copy this to .env and fill in your keys.
# Never commit .env to git.

# ── LiveKit ───────────────────────────────────────────────────────────────────
# Get these from https://cloud.livekit.io → your project → Settings → Keys
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# ── OpenAI ────────────────────────────────────────────────────────────────────
# Needed for gpt-4o-realtime-preview voice + whisper-1 transcription
OPENAI_API_KEY=sk-...

# ── Forge API (local) ─────────────────────────────────────────────────────────
FORGE_API_URL=http://localhost:8765

# ── Polygon / Alchemy (already in eliza-config.json, duplicate here for voice) ─
# ALCHEMY_KEY=your_alchemy_key
# KAITO_WALLET=0xYourWalletAddress
